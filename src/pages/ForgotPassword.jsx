import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { ArrowRight, KeyRound } from 'lucide-react'; import AuthLayout from '../components/AuthLayout'; import FormField from '../components/FormField'; import Toast from '../components/Toast'; import { api } from '../lib/api';
export default function ForgotPassword(){const nav=useNavigate();const [email,setEmail]=useState('');const [loading,setLoading]=useState(false);const [toast,setToast]=useState(null);async function submit(e){e.preventDefault();setLoading(true);try{await api('/auth/forgot-password',{method:'POST',body:JSON.stringify({email})});nav('/reset-password',{state:{email}});}catch(err){setToast({message:err.message,type:'error'});}finally{setLoading(false)}}return <AuthLayout title="Reset your password" subtitle="We'll send a one-time code to your registered email."><div className="verify-icon"><KeyRound size={27}/></div><form onSubmit={submit} className="form"><FormField label="Email" type="email" placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)} required/><button className="button primary full" disabled={loading}>{loading?'Sending code…':<>Send reset code <ArrowRight size={17}/></>}</button></form><div className="auth-footer"><Link to="/login">Back to login</Link></div><Toast toast={toast} onClose={()=>setToast(null)}/></AuthLayout>}
